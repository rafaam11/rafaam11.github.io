# CLAUDE.md

The canonical repository guidance is [AGENTS.md](AGENTS.md). Read and follow it before editing this static portfolio.

Essential checks:

```powershell
node --test
node scripts/validate-portfolio.cjs
```

Do not edit generated `public/` output for portfolio work, remove `.nojekyll`, reintroduce contribution percentages, or expose nonpublic partner names in project-facing content.
